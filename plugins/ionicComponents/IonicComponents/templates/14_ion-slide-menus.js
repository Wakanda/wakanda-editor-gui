
let componentJson = {
  manifest: {
    name: 'ion-slide-menus',
    properties:[],
    methodes: []
  },
  template: `<ion-side-menus>
  <!-- Left menu -->
  <ion-side-menu side="left">
  </ion-side-menu>

  <ion-side-menu-content>
  <!-- Main content, usually <ion-nav-view> -->
  </ion-side-menu-content>

  <!-- Right menu -->
  <ion-side-menu side="right">
  </ion-side-menu>

</ion-side-menus>`
}

export default componentJson;
